---
 - hosts: webserver
   become: yes
   gather_facts: yes
   tasks:
   - name: install tree
     apt:
      name: tree
      status: present
     tags: tag01
     when: ansible_os_family == "Debian"

   - name: install git
     apt:
      name: git
      status: present
     tags: tag02
     when: ansible_os_family == "Debian"